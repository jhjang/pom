<!-- TODO show select user info --> 
<%= form_for(@comment, :html => { :class => 'form-horizontal'})  do |comment| %>
  <%= comment.collection_select :receiver_id, User.all, :id, :name %>
  <%= comment.collection_select :category, Category.all, :name, :name %>
  <%= comment.text_field :content , :placeholder => "내용" %> 
  <%= comment.submit "칭찬하기", :class=>"btn btn-primary" %>
<% end %>

<% content_for :javascript do %>
  // comment_category 대신은?
  $("#comment_category").change(function(){
    // todo : add dynamic category
  })
<% end %>
